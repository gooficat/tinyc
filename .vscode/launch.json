{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch",
			"type": "cppdbg",
			"cwd": "${workspaceFolder}",
			"request": "launch",
			"program": "${workspaceFolder}/build/${workspaceFolderBasename}",
			"preLaunchTask": "CMake: build"
		}
	]
}
